/** @type {import('next').NextConfig} */
const nextConfig = {
    assetPrefix: "cookly-presale.github.io",
    output: "export",  // <=== enables static exports
    reactStrictMode: true,
};

module.exports = nextConfig;